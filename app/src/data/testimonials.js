/**
 * Testimonials — the section on the homepage renders ONLY when this
 * array has entries. Add real quotes as you collect them; attribution
 * by role/sector is fine (no company names needed).
 *
 * Shape:
 * {
 *     quote: 'Rahul took our pipeline from prototype to production in six weeks...',
 *     name: 'A. Sharma',            // or null to show role only
 *     role: 'Engineering Lead, enterprise AI team',
 *     sector: 'utility',            // optional, shows the sector icon
 * }
 */

export const testimonials = [];
